import { render } from "@testing-library/react";
import { CheckingAuth } from "../../../src/ui/components/CheckingAuth";

describe("test CheckingAuth component", () => {
  test("should first", () => {
    render(<CheckingAuth />);
  });
});
